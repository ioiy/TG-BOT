name = "tg-super-bot"
main = "worker.js"
compatibility_date = "2024-04-01"

# 声明项目需要的环境变量
# 一键部署时，CF 网页会自动弹窗让用户填写这两个值
[vars]
BOT_TOKEN = ""
ADMIN_ID = ""

# 声明项目需要的 KV 数据库
# 一键部署时，CF 会自动帮用户创建一个 KV 并绑定到这个变量名
[[kv_namespaces]]
binding = "KV"
id = "" # 此处必须留空，部署时由 Cloudflare 自动分配
